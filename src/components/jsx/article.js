function ArticleJSX({ darkTheme, identity }) {
    return (
      <div className={`main d-flex pt-5 align-items-center justify-content-center ${darkTheme ? 'darktheme' : ''}`} >
        ARTICLE
      </div>
    )
  }
  
  export default ArticleJSX;
  